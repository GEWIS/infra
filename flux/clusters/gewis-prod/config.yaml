apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: config
  namespace: flux-system
spec:
  dependsOn:
    - name: controllers
  interval: 1h
  retryInterval: 1m
  path: ./flux/config
  prune: true
  wait: true
  sourceRef:
    kind: GitRepository
    name: flux-system
