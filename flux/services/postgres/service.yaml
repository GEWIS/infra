apiVersion: v1
kind: Service
metadata:
  name: postgres-primary
  namespace: postgres
spec:
  type: NodePort
  selector:
    cnpg.io/cluster: postgres
    cnpg.io/instanceRole: primary
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      nodePort: 30432
